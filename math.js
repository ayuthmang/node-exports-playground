// You can export using
const add = (x, y) => x + y;
const multiply = (x, y) => x * y;

module.exports = {
  add,
  multiply
};

// or in this way
// module.exports.add = (x, y) => x + y;
// module.exports.multiply = (x, y) => x * y;
